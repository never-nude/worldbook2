# First trail: independent review

Reviewed 2026-09-22. The integration owner remains responsible for source verification and final behavior.

## DeepSeek participation

The restored local DeepSeek MCP installation completed one bounded `ask_deepseek` review using `deepseek-flash`. Input was scoped to the normalized statue case, the model adapter, and shipping/drug repair excerpts. No credentials were included. The response used 5,688 input tokens and the 1,000-token output allowance; it ended at the output limit. This was a supplied-material review, not an independent source search or a claim of exhaustive review.

Accepted findings:

- **Connection qualifications were lost in conversion.** The adapter now preserves each leg's relationship, geometry basis, limitations, and source IDs; the legacy tooltip text also includes the qualifications. A regression covers the Belgium-to-New York connection, whose collector association does not prove a Belgian shipping origin.
- **Country colors could suggest contradictory roles.** Countries now receive one neutral highlight. Numbered stops carry the sequence colors, including Cambodia's distinct departure and return records.
- **Label positions must not become evidence geography.** Tests confirm that displaced badges do not alter the actual coordinates used by connections. Geographic precision and schematic lines are stated in the reader.

The assertion that any connecting line necessarily claims physical transport was not adopted. The reader distinguishes ownership chronology, alleged movement, and repatriation, and explains that the lines do not reconstruct shipping routes. A partial shipping observation was truncated; it was not counted as a completed finding.

## Continuation review

A second bounded `deepseek-flash` review completed on 2026-09-22 through the restored MCP connection. It received the waste-candidate brief and selected reader/model excerpts, used 4,193 input and 628 output tokens, and finished normally. It did not browse sources.

Its waste-research cautions concern the distinct 103-container original-import and 69-container return populations, and the unverified vessel-transfer link. Those limitations are already recorded in the candidate brief and remain requirements for any future case; the candidate is not implemented or published. The review did not provide new corroborating evidence.

The UI suggestion to keep Whole trail active after pressing Next was not adopted. Next intentionally resumes guided focus on the new stop; toggling Whole trail itself preserves the current chapter, URL, and reading position. The integration owner retained that distinction, with existing regressions covering the preservation behavior. No code change was needed from this continuation review.

## Other independent checks

- A research helper checked the original complaint and settlement PDFs plus DOJ and UNESCO primary accounts. The packet distinguishes allegations, settlement terms, and confirmed return. It preserves the settlement's statement that the United States did not contend that Sotheby's or the collector knew of Cambodian ownership or knowingly supplied false provenance.
- A separate implementation reviewer found two startup failure paths: the reader could miss a one-time globe-ready message, and a globe finishing after 45 seconds could never reconnect. The bridge now supports an idempotent parent-initiated handshake and continued readiness detection. Focused VM regressions reproduce the original startup orders and check message origin/source boundaries.
- A separate inventory/release reviewer preserved the legacy catalogue and verified the actual production repository and `/v2/` path. Publication and write access are separate from that read-only verification.

DeepSeek is now available for bounded source extraction, contradiction checks, or substantive change review. It is not a source and is not a required gate for every edit. Avoid sending whole repositories or repeatedly asking models to agree.
