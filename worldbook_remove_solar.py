#!/usr/bin/env python3
"""Worldbook: remove the Solar System feature entirely (desktop + mobile).

Removes the full-screen "Solar System" modal - the top-bar button, the #solar page,
the dedicated Three.js orrery scene (PLANETS meshes, camera controls, play/pause,
date picker, speed slider) - and the #orrInset mini heliocentric widget (which was
already display:none!important in CSS, but the JS still wired and ticked it every
frame for nothing).

Deliberately KEEPS the separate, always-on ambient sky overlay (the faint real-time
Sun + Moon drawn over the globe via #sky/#moonCv, with correct occlusion behind
Earth) and the day/night terminator shading - neither is a "page", both are live
main-view features, and they share state/math with the master simulation clock
(solarDate, simDays, simEpoch, paused, PLANETS, julianCenturies, solveKepler) that
this patch does not touch. Confirmed via grep that none of the removed functions
are referenced by the ambient sky or the main clock loop before deleting anything.

The Sources button survives (desktop top bar unchanged); its onclick wiring, which
used to live inside the same initSolar() as the modal setup, now lives in a trimmed
initSolar() that does nothing else.
Idempotent, pure ASCII source.
Usage: python3 worldbook_remove_solar.py index.html index.html
"""
import sys

INP = sys.argv[1] if len(sys.argv) > 1 else "index.html"
OUT = sys.argv[2] if len(sys.argv) > 2 else "index.html"
text = open(INP, encoding="utf-8").read()

# Every edit here is a pure shrink (len(new) < len(old), most often new == "") with no
# append/prefix-preserving shape, so a single robust idempotency rule covers all of
# them: if "old" is still present, patch it; if it is already gone, it was already
# applied. (Each "old" string below was extracted with text.find() + a uniqueness
# check directly against the live deployed file, not hand-transcribed.)
EDITS = [
    ('slim solar-mode CSS down to sourcesBtn-only styling',
     '  /* Solar system mode */\n  .top-actions{margin-left:auto;pointer-events:auto}\n  #solarBtn,#sourcesBtn{background:var(--panel);border:1px solid var(--line);color:var(--ink);border-radius:9px;\n    padding:7px 13px;font-size:13px;cursor:pointer}\n  #solarBtn:hover,#sourcesBtn:hover{border-color:var(--accent)}\n  #solar{position:absolute;inset:0;z-index:30;display:none;\n    background:radial-gradient(circle at 50% 46%, #0b1330 0%, #060a16 45%, #03050b 100%)}\n  #solar.open{display:block}\n  #solar canvas{width:100%;height:100%;display:block;cursor:grab}\n  #solarTitle{position:absolute;top:16px;left:20px;font-size:15px;font-weight:650;letter-spacing:.3px;\n    color:var(--ink);pointer-events:none}\n  #solarTitle small{display:block;color:var(--muted);font-size:11.5px;font-weight:400;letter-spacing:.2px;margin-top:2px}\n  .solar-ui{position:absolute;bottom:20px;left:50%;transform:translateX(-50%);display:flex;gap:9px;align-items:center;\n    background:rgba(16,21,30,.92);border:1px solid var(--line);border-radius:13px;padding:9px 12px;backdrop-filter:blur(10px)}\n  .solar-ui .sbtn{background:var(--chip);color:var(--ink);border:1px solid var(--line);border-radius:9px;\n    padding:7px 12px;font-size:13px;cursor:pointer}\n  .solar-ui .sbtn:hover{border-color:var(--accent)}\n  .solar-ui input[type=date]{background:var(--chip);color:var(--ink);border:1px solid var(--line);\n    border-radius:9px;padding:6px 9px;font-size:13px;color-scheme:dark}\n  #solarDateLbl{color:var(--muted);font-size:12.5px;min-width:60px}\n  .planet-label{position:absolute;transform:translate(-50%,-170%);color:#c4cfdd;font-size:11.5px;\n    pointer-events:none;white-space:nowrap;text-shadow:0 0 4px #000,0 0 4px #000;z-index:2}\n  .planet-label.earth{color:#dce8ff;font-weight:600}\n',
     '  .top-actions{margin-left:auto;pointer-events:auto}\n  #sourcesBtn{background:var(--panel);border:1px solid var(--line);color:var(--ink);border-radius:9px;\n    padding:7px 13px;font-size:13px;cursor:pointer}\n  #sourcesBtn:hover{border-color:var(--accent)}\n'),
    ('remove dead #orrInset CSS (widget was already display:none!important)',
     '  /* heliocentric inset orrery \u2014 removed from the UI */\n  #orrInset{display:none!important;position:absolute;right:14px;bottom:72px;z-index:5;width:190px;background:var(--panel);\n    border:1px solid var(--line);border-radius:13px;padding:9px 11px 7px;backdrop-filter:blur(10px)}\n  #orrInset canvas{width:168px;height:168px;display:block;margin-top:2px}\n  #orrInset.collapsed canvas{display:none}\n  #orrInset.collapsed .panel-head{padding-bottom:0}\n',
     ''),
    ('drop now-pointless mobile #orrInset override',
     '    /* declutter: drop the zoom +/- (pinch handles it) and the orrery inset on phones */\n    .maplibregl-ctrl-group{display:none!important}\n    #orrInset{display:none}\n',
     '    /* declutter: drop the zoom +/- (pinch handles it) */\n    .maplibregl-ctrl-group{display:none!important}\n'),
    ('remove #orrInset markup',
     '<div id="orrInset">\n  <div class="panel-head" id="orrHead">Solar system \xb7 heliocentric<span class="caret">\u25be</span></div>\n  <canvas id="orrCv"></canvas>\n</div>\n',
     ''),
    ('remove Solar system button from top bar',
     '    <button id="sourcesBtn">Sources</button>\n    <button id="solarBtn">Solar system</button>\n',
     '    <button id="sourcesBtn">Sources</button>\n'),
    ('remove full-screen Solar System page markup',
     '<div id="solar">\n  <canvas id="orrery"></canvas>\n  <div id="solarTitle">The Solar System<small id="solarSub"></small></div>\n  <div class="solar-ui">\n    <button class="sbtn" id="solarBack">\u2295 Enter Worldbook</button>\n    <button class="sbtn" id="playBtn">\u23f8 Pause</button>\n    <button class="sbtn" id="todayBtn">Now</button>\n    <input type="date" id="solarDate">\n    <span id="solarDateLbl"></span>\n    <label style="font-size:12px;color:var(--muted);margin-left:6px">Speed</label>\n    <input type="range" id="spinSlider" min="0.1" max="120" step="0.1" value="1" style="width:120px;accent-color:#6ea8fe">\n    <span id="spinLbl" style="font-size:12px;color:var(--muted);min-width:80px">1.0 days / min</span>\n  </div>\n</div>\n\n',
     ''),
    ('drop boot-time #orrInset wiring',
     '    sizeOrr(); drawOrr();\n    { const oh=document.getElementById("orrHead"); if(oh) oh.onclick=()=>document.getElementById("orrInset").classList.toggle("collapsed");\n      if(window.innerWidth<=700) document.getElementById("orrInset").classList.add("collapsed"); }\n    window.addEventListener("resize",()=>{ sizeOrr(); drawOrr(); });\n',
     ''),
    ('drop orrOn probe in master clock',
     '  const orrOn=(()=>{const e=document.getElementById("orrInset");return e&&getComputedStyle(e).display!=="none";})();\n',
     ''),
    ('drop drawOrr() call from per-frame loop',
     '    if(orrOn) drawOrr();\n',
     ''),
    ('remove sizeOrr/drawOrr (orrInset-exclusive)',
     'function sizeOrr(){ const cv=document.getElementById("orrCv"); if(!cv)return; const dpr=Math.min(window.devicePixelRatio||1,2);\n  cv.width=168*dpr; cv.height=168*dpr; const x=cv.getContext("2d"); if(x) x.setTransform(dpr,0,0,dpr,0,0); }\nfunction drawOrr(){\n  const cv=document.getElementById("orrCv"); if(!cv)return; const x=cv.getContext("2d"); if(!x)return;\n  const W=168,cx=W/2,cy=W/2; x.clearRect(0,0,W,W);\n  const maxR=W/2-8, scale=r=>maxR*Math.pow(r,0.5)/Math.pow(30.3,0.5), T=julianCenturies(solarDate);\n  x.fillStyle="#ffd24d"; x.beginPath(); x.arc(cx,cy,3.2,0,6.2832); x.fill();\n  PLANETS.forEach(p=>{ const el=elemAt(p,T);\n    x.strokeStyle="rgba(174,190,224,0.5)"; x.lineWidth=1.1; x.beginPath();\n    for(let a=0;a<=360;a+=8){ const q=orbXY(el,a*DEG); const sx=cx+scale(q.r)*q.x/q.r, sy=cy-scale(q.r)*q.y/q.r; a===0?x.moveTo(sx,sy):x.lineTo(sx,sy); }\n    x.closePath(); x.stroke();\n    const q=planetXY(p,T), px=cx+scale(q.r)*q.x/q.r, py=cy-scale(q.r)*q.y/q.r, isE=p.name==="Earth";\n    if(isE){ x.strokeStyle="#9cc3ff"; x.lineWidth=1.2; x.beginPath(); x.arc(px,py,4,0,6.2832); x.stroke(); }\n    x.fillStyle=isE?"#6ea8fe":p.color; x.beginPath(); x.arc(px,py,isE?2.4:1.8,0,6.2832); x.fill();\n  });\n}\n',
     ''),
    ('remove full-screen Solar System modal (Three.js scene, controls, state) - keeps Sources button wiring',
     'function toISO(d){ return d.toISOString().slice(0,10); }\nfunction spinLabel(s){ return s.toFixed(1)+" days / min"; }\nfunction initSolar(){\n  const di=document.getElementById("solarDate"); di.value=toISO(solarDate);\n  di.addEventListener("change",()=>{ const d=new Date(di.value+"T12:00:00Z"); if(!isNaN(d.getTime())){ simEpoch=d.getTime(); simDays=0; simLast=0; solarDate=d; updatePlanets(); } });\n  document.getElementById("playBtn").onclick=togglePlay;\n  document.getElementById("todayBtn").onclick=()=>{ simEpoch=Date.now(); simDays=0; simLast=0; solarDate=new Date(); di.value=toISO(solarDate); updatePlanets(); };\n  document.getElementById("solarBack").onclick=closeSolar;\n  { const b=document.getElementById("solarBtn"); if(b) b.onclick=openSolar; }\n  { const b=document.getElementById("sourcesBtn"); if(b) b.onclick=openSourcesIndex; }\n  const sl=document.getElementById("spinSlider");\n  if(sl) sl.addEventListener("input",()=>{ simSpeed=+sl.value; const l=document.getElementById("spinLbl"); if(l)l.textContent=spinLabel(simSpeed); });\n  window.addEventListener("resize",()=>{ if(solarOpen) resize3D(); });\n}\nconst AUk=26, SUNU=9;\nfunction auScale(r){ return AUk*Math.pow(r,0.62); }       // compressed radial distance (scene units)\nconst MOONS=[\n  {host:2,name:"Moon",     r:5.5, period:27.32,size:0.8, color:0xcfcfcf},\n  {host:4,name:"Io",       r:11,  period:1.77, size:0.7, color:0xdacb7a},\n  {host:4,name:"Europa",   r:13.5,period:3.55, size:0.65,color:0xd2d4d0},\n  {host:4,name:"Ganymede", r:16.5,period:7.15, size:0.85,color:0xb9a98f},\n  {host:4,name:"Callisto", r:20,  period:16.69,size:0.8, color:0x8d8377},\n  {host:5,name:"Titan",    r:13,  period:15.95,size:0.8, color:0xc9a35a}\n];\nfunction build3D(){\n  if(TH) return true;\n  if(typeof THREE==="undefined") return false;\n  const cv=document.getElementById("orrery");\n  const renderer=new THREE.WebGLRenderer({canvas:cv,antialias:true});\n  renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));\n  renderer.setClearColor(0x04060c,1);\n  const scene=new THREE.Scene();\n  const camera=new THREE.PerspectiveCamera(55,1,0.1,40000);\n  scene.add(new THREE.AmbientLight(0x223046,0.32));\n  scene.add(new THREE.PointLight(0xfff2d0,2.9,0,0));      // the Sun lights everything\n  scene.add(new THREE.Mesh(new THREE.SphereGeometry(SUNU,40,40),new THREE.MeshBasicMaterial({color:0xffd24d})));\n  scene.add(new THREE.Mesh(new THREE.SphereGeometry(SUNU*1.7,32,32),new THREE.MeshBasicMaterial({color:0xffb12e,transparent:true,opacity:0.16})));\n  // native 3D starfield \u2014 no banding\n  const N=4200, arr=new Float32Array(N*3);\n  for(let i=0;i<N;i++){ const rr=8000+Math.random()*8000, th=Math.random()*6.2832, ph=Math.acos(2*Math.random()-1);\n    arr[i*3]=rr*Math.sin(ph)*Math.cos(th); arr[i*3+1]=rr*Math.cos(ph); arr[i*3+2]=rr*Math.sin(ph)*Math.sin(th); }\n  const stg=new THREE.BufferGeometry(); stg.setAttribute("position",new THREE.BufferAttribute(arr,3));\n  scene.add(new THREE.Points(stg,new THREE.PointsMaterial({color:0xffffff,size:2,sizeAttenuation:false})));\n  // planets (real spheres), orbits, labels\n  const meshes=[], labels=[], T0=julianCenturies(solarDate); let earth=null;\n  PLANETS.forEach((p,i)=>{\n    const isEarth=p.name==="Earth";\n    const rad=isEarth?3.4:Math.max(2.4,p.rad*0.7);\n    const mesh=new THREE.Mesh(new THREE.SphereGeometry(rad,isEarth?48:32,isEarth?48:32),\n      new THREE.MeshStandardMaterial({color:isEarth?0x2b6fb0:p.color,roughness:1,metalness:0}));\n    if(isEarth) mesh.rotation.z=23.4*DEG;\n    scene.add(mesh); meshes.push(mesh); if(isEarth)earth=mesh;\n    const pts=[], el=elemAt(p,T0);\n    for(let a=0;a<=360;a+=2){ const q=orbXY(el,a*DEG); pts.push(new THREE.Vector3(auScale(q.r)*q.x/q.r,0,-auScale(q.r)*q.y/q.r)); }\n    scene.add(new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(pts),\n      new THREE.LineBasicMaterial({color:0x6f86ad,transparent:true,opacity:0.28})));\n    if(p.name==="Saturn"){ const ring=new THREE.Mesh(new THREE.RingGeometry(rad*1.4,rad*2.3,56),\n        new THREE.MeshBasicMaterial({color:0xcdb98a,side:THREE.DoubleSide,transparent:true,opacity:0.55}));\n      ring.rotation.x=Math.PI/2.3; mesh.add(ring); }\n    const d=document.createElement("div"); d.className="planet-label"+(isEarth?" earth":"");\n    d.textContent=p.name+(isEarth?" \u25c4 click to enter":""); document.getElementById("solar").appendChild(d); labels.push(d);\n  });\n  // Earth texture (opaque oceans); falls back to the blue sphere if it can\'t load\n  try{ const tl=new THREE.TextureLoader(); if(tl.setCrossOrigin)tl.setCrossOrigin("anonymous");\n    tl.load("https://cdn.jsdelivr.net/gh/mrdoob/three.js@r128/examples/textures/planets/earth_atmos_2048.jpg",\n      t=>{ if(earth){ earth.material.map=t; earth.material.color.set(0xffffff); earth.material.needsUpdate=true; } },undefined,()=>{}); }catch(_){}\n  // moons + their orbit rings, parented to host groups that ride along with each planet\n  const moonHosts={}, moons=[];\n  MOONS.forEach(mn=>{ if(!moonHosts[mn.host]){ const g=new THREE.Group(); scene.add(g); moonHosts[mn.host]=g; }\n    const host=moonHosts[mn.host];\n    const m=new THREE.Mesh(new THREE.SphereGeometry(mn.size,18,18),new THREE.MeshStandardMaterial({color:mn.color,roughness:1}));\n    host.add(m);\n    const rp=[]; for(let a=0;a<=360;a+=6) rp.push(new THREE.Vector3(Math.cos(a*DEG)*mn.r,0,Math.sin(a*DEG)*mn.r));\n    host.add(new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(rp),new THREE.LineBasicMaterial({color:0x5a6b86,transparent:true,opacity:0.3})));\n    moons.push({mesh:m,host:mn.host,r:mn.r,period:mn.period}); });\n  const ctrl={theta:0.85,phi:1.0,radius:auScale(30)*1.4,target:new THREE.Vector3(0,0,0)};\n  TH={renderer,scene,camera,meshes,labels,earth,moonHosts,moons,ctrl};\n  attachControls(cv); updatePlanets(); resize3D();\n  return true;\n}\nfunction camPos(){ const c=TH.ctrl,cam=TH.camera;\n  cam.position.set(c.target.x+c.radius*Math.sin(c.phi)*Math.cos(c.theta),\n                   c.target.y+c.radius*Math.cos(c.phi),\n                   c.target.z+c.radius*Math.sin(c.phi)*Math.sin(c.theta));\n  cam.lookAt(c.target); }\nfunction attachControls(cv){\n  let drag=false,lx=0,ly=0;\n  cv.addEventListener("pointerdown",e=>{drag=true;lx=e.clientX;ly=e.clientY;try{cv.setPointerCapture(e.pointerId);}catch(_){}});\n  cv.addEventListener("pointerup",()=>{drag=false;});\n  cv.addEventListener("pointermove",e=>{ if(!drag)return; const dx=e.clientX-lx,dy=e.clientY-ly;lx=e.clientX;ly=e.clientY;\n    TH.ctrl.theta-=dx*0.005; TH.ctrl.phi=Math.max(0.18,Math.min(2.96,TH.ctrl.phi-dy*0.005)); });\n  cv.addEventListener("wheel",e=>{ e.preventDefault();\n    TH.ctrl.radius=Math.max(auScale(1)*0.5,Math.min(auScale(30)*4.5,TH.ctrl.radius*(1+(e.deltaY>0?0.09:-0.09)))); },{passive:false});\n  cv.addEventListener("click",e=>{ const r=cv.getBoundingClientRect();\n    const m=new THREE.Vector2(((e.clientX-r.left)/r.width)*2-1,-((e.clientY-r.top)/r.height)*2+1);\n    const rc=new THREE.Raycaster(); rc.setFromCamera(m,TH.camera);\n    if(rc.intersectObject(TH.meshes[2]).length) closeSolar(); });\n}\nfunction updatePlanets(){\n  if(!TH) return; const T=julianCenturies(solarDate);\n  PLANETS.forEach((p,i)=>{ const q=planetXY(p,T); const x=auScale(q.r)*q.x/q.r, z=-auScale(q.r)*q.y/q.r;\n    TH.meshes[i].position.set(x,0,z); if(TH.moonHosts[i]) TH.moonHosts[i].position.set(x,0,z); });\n  document.getElementById("solarDateLbl").textContent=solarDate.toLocaleDateString(undefined,{year:"numeric",month:"short",day:"numeric"});\n  document.getElementById("solarSub").textContent="Real-time solar system \xb7 "+solarDate.toLocaleString(undefined,{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit"});\n}\nfunction updateMoons(){ if(!TH)return; TH.moons.forEach(mn=>{ const a=(simDays/mn.period)*2*Math.PI; mn.mesh.position.set(Math.cos(a)*mn.r,0,Math.sin(a)*mn.r); }); }\nfunction resize3D(){ if(!TH)return; const cv=TH.renderer.domElement,w=cv.clientWidth,h=cv.clientHeight;\n  if(!w||!h)return; TH.renderer.setSize(w,h,false); TH.camera.aspect=w/h; TH.camera.updateProjectionMatrix(); }\nfunction renderSolar(){\n  if(!solarOpen||!TH) return;\n  const now=performance.now(); if(!simLast)simLast=now; const dt=(now-simLast)/1000; simLast=now;\n  if(!paused){ simDays += dt*(simSpeed/60); solarDate=new Date(simEpoch+simDays*86400000); updatePlanets(); }\n  if(TH.earth) TH.earth.rotation.y = simDays*2*Math.PI;     // 1 rotation per simulated day\n  updateMoons();\n  TH.ctrl.theta+=0.0004; camPos();\n  const cv=TH.renderer.domElement,w=cv.clientWidth,h=cv.clientHeight;\n  TH.meshes.forEach((mesh,i)=>{ const v=mesh.position.clone().project(TH.camera); const lab=TH.labels[i];\n    if(v.z>1){lab.style.display="none";return;} lab.style.display="block";\n    lab.style.left=((v.x*0.5+0.5)*w)+"px"; lab.style.top=((-v.y*0.5+0.5)*h)+"px"; });\n  TH.renderer.render(TH.scene,TH.camera);\n  solarRAF=requestAnimationFrame(renderSolar);\n}\nfunction openSolar(){ solarOpen=true; document.getElementById("solar").classList.add("open");\n  if(!build3D()){ closeSolar(); return; }\n  if(!simEpoch){ simEpoch=Date.now(); simDays=0; solarDate=new Date(); }\n  simLast=0; resize3D(); renderSolar(); }\nfunction closeSolar(){ solarOpen=false;\n  if(solarRAF){cancelAnimationFrame(solarRAF);solarRAF=null;} document.getElementById("solar").classList.remove("open"); lastInteract=Date.now(); }\nfunction togglePlay(){ paused=!paused; document.getElementById("playBtn").textContent=paused?"\u25b6 Resume":"\u23f8 Pause"; if(!paused)simLast=0; }',
     'function initSolar(){\n  { const b=document.getElementById("sourcesBtn"); if(b) b.onclick=openSourcesIndex; }\n}'),
    ('drop solar-system claim from name="description" meta tag',
     '<meta name="description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration, plus a real-time solar system." />',
     '<meta name="description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration." />'),
    ('drop solar-system claim from property="og:description" meta tag',
     '<meta property="og:description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration, plus a real-time solar system." />',
     '<meta property="og:description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration." />'),
    ('drop solar-system claim from name="twitter:description" meta tag',
     '<meta name="twitter:description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration, plus a real-time solar system." />',
     '<meta name="twitter:description" content="Worldbook is an interactive 3D globe reference work: sourced data on every country across religion, politics, economy, health, debt, trade, and migration." />'),
]

res = []
for name, old, new in EDITS:
    if old in text:
        text = text.replace(old, new, 1)
        res.append((name, "patched"))
    else:
        res.append((name, "already-applied"))

open(OUT, "w", encoding="utf-8").write(text)
for name, s in res:
    print(f"  [{s:>16}] {name}")
print("OK: Solar System feature removed")
